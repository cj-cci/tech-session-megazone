version: 2.1

jobs:
  build-frontend-assets:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: Run Asset Builder (Will Fail with Permission Denied)
          # 스크립트에 실행 권한이 없으므로 CI 환경에서 에러를 뱉고 멈춥니다.
          command: ./scripts/build-test.sh

workflows:
  permission-demo:
    jobs:
      - build-frontend-assets