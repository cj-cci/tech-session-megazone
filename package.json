{
    "name": "circleci-demo",
    "version": "1.0.0",
    "scripts": {
      "test": "jest",
      "test:ci": "jest --ci --runInBand",
      "build": "node scripts/simulate-build.js"
    },
    "dependencies": {
      "express": "^4.18.2"
    },
    "devDependencies": {
      "jest": "^29.7.0",
      "jest-junit": "^16.0.0"
    },
    "jest": {
      "reporters": [
        "default",
        "jest-junit"
      ]
    }
  }